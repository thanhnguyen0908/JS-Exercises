const HOST = ''; // HOST_PRODUCT
const HOST_DEV = 'http://45.77.33.125:8000/api/v1'; // HOST_DEV
const HOST_URL = '';

module.exports = {
  HOST_URL: HOST_URL,
  HOST: HOST,
  HOST_DEV: HOST_DEV,
  TIME_OUT_REQUEST_API: 30000,
  // true build apk debug, false build apk release
  logEnabled: true,
};
